[package]
name = "gitchange"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
gitchange-core.workspace = true
gitchange-tui.workspace = true
clap.workspace = true
anyhow.workspace = true

[dev-dependencies]
tempfile.workspace = true
# The `--json` faces are asserted as parsed documents, never as strings:
# a test that pinned the serialised bytes would pin field order too.
serde_json.workspace = true
