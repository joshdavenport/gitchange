Two actors' hunks sit unassigned before either assigns, so the first `assign <path>` can sweep a concurrent actor's hunks; the guard fires for the second actor, and recovery is human coordination — reclaiming from another changelist stays forbidden (ADR 0015 contract). Edits within context distance merge into one hunk no address can split. Net: the multi-actor scope boundary moves from same-file to same-region; ADR 0015's boundary sentence is amended when hunk-level assign lands.
