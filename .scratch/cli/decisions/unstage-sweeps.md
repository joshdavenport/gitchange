The clean inverse of `add` (`●` and `◑` alike go index := HEAD) was considered and declined: unstage is a rare action for an agent, so core's `●`-only bulk filter stands and the asymmetry with `add`'s `○`+`◑` sweep is accepted. The residue is never silent — a sweep that keeps a `◑` names it in the receipt with both resolutions (unstage by address, or `add` then `unstage`). An addressed `◑` unstages ungated, index-only content included, though that discard is irrecoverable: the TUI's `space` and the reserved `add` already make the identical index write to the same hunk without a guard, and guarding one direction of the same write would teach a false asymmetry; the skill's staleness-family bullet (`index_only: true`) is the mitigation. Per-hunk `◑` unstage exceeds TUI reachability — the toggle sends every `◑` to the stage direction — which op parity permits: parity is about core ops, and core's `unstage_hunk` already makes this write.
