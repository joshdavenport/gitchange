A hunk ID carries no author, so an ID-first flow makes the agent read every hunk in the file (`diff --json`) and match content itself — the author is the only party that knows which hunks are theirs. `--containing` inverts it: the agent supplies a distinctive line it wrote and gitchange does the matching, exactly. Hunk IDs stay the canonical address underneath — refusals and receipts speak them. Prior art: `git add -p`'s `/` (git's only interactive hunk addressing, and it is by content), pickaxe (`git log -S`).
