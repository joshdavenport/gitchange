### gitchange

`gitchange [-C <dir>]` — neither: bare invocation launches the TUI, whose engine refreshes persist

- bare invocation **[built]** — no subcommand launches the TUI; the global `-C` (see the preface) aims the TUI at another worktree

## Decisions
